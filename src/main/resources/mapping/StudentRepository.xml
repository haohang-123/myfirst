<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haohang.repository.StudentRepository">

    <select id="findAll" resultType="Student">
        select * from student
    </select>

    <insert id="add" parameterType="student">
        insert into student(name, gender, age) values (#{name}, #{gender}, #{age})
    </insert>

    <update id="update" parameterType="student">
        update student set name = #{name}, gender = #{gender}, age = #{age} where id = #{id}
    </update>

    <select id="findById" parameterType="java.lang.Integer" resultType="student">
        select id, name, gender, age from student where id = #{id}
    </select>
</mapper>